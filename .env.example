# Authentication (used by packages/auth)
# Must be a long, random string (at least 32 characters)
SESSION_SECRET=""

# Database (used by packages/auth-db)
# Connection string for your PostgreSQL database
# Example: postgresql://user:password@host:port/database
POSTGRES_URL=""

# Application Port (used by apps/web for SSR, potentially other apps)
# Default port the web application runs on
PORT=3000

# AI Provider API Keys (used by packages/api -> packages/ai)
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
GEMINI_API_KEY=""
DEEPSEEK_API_KEY="" 